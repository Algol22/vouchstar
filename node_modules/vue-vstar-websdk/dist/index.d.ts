declare module 'vue-vstar-websdk' {
  import { Component } from 'vue';

  export const VouchstarModal: Component;
  export const VouchstarBtn: Component;

  interface VouchstarOptions {
    vouchstarToken?: string;
    vouchstarDomain?: string;
  }

  const VouchstarWebsdk: {
    install: (app: App, options?: VouchstarOptions) => void;
  };

  export default VouchstarWebsdk;
}
